# 已知问题

## 2026-07-11：多份材料的字段展示不完整（已修复）

现象：多份材料同时上传时，个别文件已识别为多页，但右侧逐页字段区只显示含字段的第一页。

原因：后端仅依据 OCR 返回的页面和字段页面建立分组；当 OCR 结果缺少某些空页时，已上传文件的其余页不会进入右侧页面列表。

处理：字段分组现在始终按上传文件的总页数建立第 1 至第 N 页；已识别字段仍在对应页展示，未提取字段的页面也会保留在列表中。
